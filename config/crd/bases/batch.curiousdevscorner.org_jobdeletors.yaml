---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: jobdeletors.batch.curiousdevscorner.org
spec:
  group: batch.curiousdevscorner.org
  names:
    kind: JobDeletor
    listKind: JobDeletorList
    plural: jobdeletors
    singular: jobdeletor
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.jobsDeleted
      name: Jobs Deleted
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: JobDeletor is the Schema for the jobdeletors API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              checkIntervalSeconds:
                default: 3600
                description: 'CheckIntervalSeconds is how often to check for old Jobs
                  (default: 3600)'
                format: int32
                minimum: 60
                type: integer
              maxAge:
                description: |-
                  MaxAge defines how old a Job can be before deletion
                  Example values: "10m", "2h", "7d"
                pattern: ^[0-9]+(s|m|h|d)$
                type: string
              namespaces:
                description: |-
                  Namespaces is the list of namespaces to watch for Jobs
                  If empty, watches all namespaces
                items:
                  type: string
                type: array
              selector:
                additionalProperties:
                  type: string
                description: |-
                  Selector allows filtering Jobs by labels
                  If empty, all Jobs are candidates for deletion
                type: object
            required:
            - maxAge
            type: object
          status:
            description: JobDeletorStatus defines the observed state of JobDeletor
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the JobDeletor resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              jobsDeleted:
                description: JobsDeleted is the total number of Jobs deleted by this
                  JobDeletor
                format: int32
                type: integer
              lastCheckTime:
                description: LastCheckTime is when we last checked for Jobs to delete
                format: date-time
                type: string
              lastDeletedJobs:
                description: LastDeletedJobs lists the most recent Jobs deleted (max
                  10)
                items:
                  type: string
                type: array
              message:
                description: Message provides human-readable status information
                type: string
              phase:
                description: Phase represents the current phase of the JobDeletor
                enum:
                - Starting
                - Active
                - Failed
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.jobsDeleted
      name: Jobs Deleted
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: JobDeletor is the Schema for the jobdeletors API (v1beta1).
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: JobDeletorSpec defines the desired state of JobDeletor in
              v1beta1.
            properties:
              checkIntervalSeconds:
                default: 3600
                description: |-
                  CheckIntervalSeconds is how often (in seconds) to check for old Jobs.
                  Defaults to 3600 (1 hour). Minimum is 60 seconds.
                format: int32
                minimum: 60
                type: integer
              maxAge:
                description: |-
                  MaxAge defines how old a Job must be before it is eligible for deletion.
                  This replaces the v1alpha1 free-form string field with a structured type
                  for clarity and stronger validation.
                  Example: {value: 2, unit: Hours} means delete jobs older than 2 hours.
                properties:
                  unit:
                    description: |-
                      Unit is the time unit for Value.
                      Valid values are: Seconds, Minutes, Hours, Days.
                    enum:
                    - Seconds
                    - Minutes
                    - Hours
                    - Days
                    type: string
                  value:
                    description: Value is the numeric amount of time.
                    format: int32
                    minimum: 1
                    type: integer
                required:
                - unit
                - value
                type: object
              namespaces:
                description: |-
                  Namespaces is the list of namespaces to watch for Jobs.
                  If empty, all namespaces are watched.
                items:
                  type: string
                type: array
              selector:
                description: |-
                  Selector allows filtering Jobs by label selectors.
                  v1beta1 upgrades this from a plain map[string]string to a proper
                  metav1.LabelSelector, which supports both matchLabels and matchExpressions.
                  This is the idiomatic Kubernetes API pattern used by Deployments, ReplicaSets, etc.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
            required:
            - maxAge
            type: object
          status:
            description: |-
              JobDeletorStatus defines the observed state of JobDeletor.
              Status fields are intentionally kept identical between v1alpha1 and v1beta1
              to ensure lossless round-trip conversion. Adding new status fields in a
              spoke version would require storing them in an annotation during ConvertTo
              (the "annotation round-trip" pattern) — we avoid that complexity here.
            properties:
              conditions:
                description: Conditions represent the current state of the JobDeletor
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              jobsDeleted:
                description: JobsDeleted is the total number of Jobs deleted by this
                  JobDeletor.
                format: int32
                type: integer
              lastCheckTime:
                description: LastCheckTime is when we last checked for Jobs to delete.
                format: date-time
                type: string
              lastDeletedJobs:
                description: LastDeletedJobs lists the most recent Jobs deleted (max
                  10).
                items:
                  type: string
                type: array
              message:
                description: Message provides human-readable status information.
                type: string
              phase:
                description: Phase represents the current phase of the JobDeletor.
                enum:
                - Starting
                - Active
                - Failed
                type: string
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
