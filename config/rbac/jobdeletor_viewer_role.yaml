# This rule is not used by the project jobdeletor-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to batch.curiousdevscorner.org resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: jobdeletor-operator
    app.kubernetes.io/managed-by: kustomize
  name: jobdeletor-viewer-role
rules:
- apiGroups:
  - batch.curiousdevscorner.org
  resources:
  - jobdeletors
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - batch.curiousdevscorner.org
  resources:
  - jobdeletors/status
  verbs:
  - get
